export type ColorMode = "light" | "dark";

/** Accent palette (Teal default vs Indigo comparison route). */
export type BrandAccent = "teal" | "indigo";

export type ThemeCssVars = Record<string, string>;
